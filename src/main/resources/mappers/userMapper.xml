<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kosta.matchmaker.mappers.UserMapper">

	<insert id="join">
		insert into user(userid, userpw, username, email, regdate, userage, userinfo)
		 values(#{userid}, #{userpw}, #{username}, #{email}, now(), #{userage}, #{userinfo});
	</insert>
	
	<select id="selectList" resultType="UserVO">
		select * from user
	</select>
	
	<select id="selectOne"  resultType="UserVO">
		select * from user where userid = #{userid} 
		<!-- and userpw = #{userpw} -->
	</select>
	
	<update id="update">
		update user set username = #{username}, email = #{email}
		where userid = #{userid}
	</update>
	
	<delete id="delete">
		delete from user where userid = #{userid} and userpw = #{userpw}
	</delete>
	
	<select id="selectId" resultType="UserVO">
		select * 
		from user 
		where userid = #{userid} 
	</select>
	
	
</mapper>